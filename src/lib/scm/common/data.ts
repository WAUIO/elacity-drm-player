export enum DistributionMethod {
  ON_DEMAND_STREAM = 'on-demand-stream',
  DIGITAL_SALE = 'digital-sale',
}

export enum Label {
  BIG = 'big-labels',
  INDIE = 'indie-labels',
  SELF = 'no-labels', // no-labels
}
